# Names only. Real values live in .env.local (gitignored) and in Vercel's env vars.
# Both of these are public-by-design keys. The service-role / secret key is never
# used by this app and must never appear here, in the repo, or in Vercel.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
